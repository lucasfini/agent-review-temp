# Manual PyAnnote Installation
# Use this if the automated script fails

# Step 1: Install PyTorch first
# pip install torch torchaudio --index-url https://download.pytorch.org/whl/cpu

# Step 2: Install these dependencies
soundfile>=0.12.0
librosa>=0.10.0
transformers>=4.30.0
accelerate>=0.20.0

# Step 3: Install PyAnnote
# pip install pyannote.audio>=3.1.0